#ifndef CONSTS_H
#define CONSTS_H

const int CELL_SIZE = 36;
const int GRID_WIDTH = 9;
const int GRID_HEIGHT = 9;

const static int SCREEN_WIDTH = (GRID_WIDTH * CELL_SIZE) + 1;
const static int SCREEN_HEIGHT = (GRID_HEIGHT * CELL_SIZE) + 1;

#endif